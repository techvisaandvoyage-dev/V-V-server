# Used automatically by `vite build` (production). Safe to commit for this deployment.
VITE_API_URL=https://server-x358.onrender.com

# After your admin app is on Cloudflare Pages, set the full admin origin (no trailing slash).
# Uncomment and replace with your real URL or set in Cloudflare env instead:
# VITE_ADMIN_APP_ORIGIN=https://your-admin-project.pages.dev
